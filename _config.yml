theme: jekyll-theme-cayman

title: "x Founders Legion x"
domain: founderslegion.com       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://founderslegion.com  # the base hostname and protocol for your site, e.g. http://example.com
# baseurl: /REPOSITORY-NAME/      # place folder name if the site is served in a subfolder
